{% extends 'base.html.twig' %}

{% block title %}Profesores{% endblock %}

{% block body %}
    <h1>Profesores</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profesore.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ profesore.nombre }}</td>
            </tr>
            <tr>
                <th>Apellido</th>
                <td>{{ profesore.apellido }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_profesores_index') }}">back to list</a>

    <a href="{{ path('app_profesores_edit', {'id': profesore.id}) }}">edit</a>

    {{ include('profesores/_delete_form.html.twig') }}
{% endblock %}

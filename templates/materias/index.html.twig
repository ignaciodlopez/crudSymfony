{% extends 'base.html.twig' %}

{% block title %}Materias CRUD{% endblock %}

{% block body %}
<div class = "container p-4">
    <h1>Materias CRUD</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for materia in materias %}
            <tr>
                <td>{{ materia.id }}</td>
                <td>{{ materia.nombre }}</td>
                <td>
                    <a href="{{ path('app_materias_show', {'id': materia.id}) }}">Mostrar</a>
                    <a href="{{ path('app_materias_edit', {'id': materia.id}) }}">Editar</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">No se encontraron datos :/</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_materias_new') }}">Ingresar una Materia</a>
</div>
{% endblock %}
